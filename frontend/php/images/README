For each usual icons, two sizes availables are prefered:
	$file.orig.png = 48x48
	$file.png = 24x24

The $file.png can be regenerated typing
	make icons

AFAIK, the icons come from the GNOME and KDE projects, and are free as
in freedom.


- Similar icons:

FILES=`find -type d -name ".svn" -prune -o -print | grep -v .orig.`
for file in $FILES; do
    for file2 in $FILES; do
	if [ $file != $file2 ]; then
	    if diff -q $file $file2 >/dev/null; then
		echo $file = $file2
		break
	    fi
	fi
    done
done > trace.txt

error.orig.png = wrong.orig.png
=> 'error' unused in the code, now replaced by 'wrong'
=> removing error.*.png

ftp.orig.png = download.orig.png
=> idem, 'ftp' unused and replaced by 'download', removing 'ftp'


- Removing generated icons:

find -name "*.orig.png" | while read file; do rm ${file%.orig.png}.png; done


- Visually check icons:

find -name www.orig.png -type f | while read file; do gqview $file& done


- All the versions of an image in a single directory, suitable for cross-theme comparison:

find `find .. -maxdepth 1 -type d` -maxdepth 1 -name "*.png" -type f | while read i; do ln -s $i `basename $i | sed 's/.png$//'`-`echo $i | perl -p -e 's,../([^/]*)/.*,$1,'`.png; done


- Image without a .orig counterpart:

find -name "*.png" | grep -v orig.png | while read image; do if [ ! -e ${image%.png}.orig.png ]; then echo $image; fi; done | xargs -n1 basename | sort | uniq

blank.png
desktop.orig-2.png
floating.png
gnalogo.png
hand.right.png
icon.png
leaves.png
leopard.png
menu.png
people.orig-2.png
point.png
sand.png
steel.png

- Apply a file to symlink change in SVN:

for i in `svn st | awk '/^~/{print $2}'`; do
    mv $i $i-old
    svn del $i
    mv $i-old $i
    svn add $i
done

- Groups:

  * Arrows

  * Roles

- Factor mime_types

- Rename arrows with a more descriptive name
$ ls -d */arrows|xargs -n1 readlink|sort |uniq -c
     15 ../common/arrows1
      6 ../common/arrows2
      5 ../common/arrows3

- TODO:

* For:
../DarkSlate.theme
../Light.theme
../OldCERN.theme
../pllx.theme
../Right.theme


- Make both Savannah's and Savanedu's floating.png transparent and merge the 2 image directories
